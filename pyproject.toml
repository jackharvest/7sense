[project]
name = "7sense"
version = "1.0.0"
description = "Archive extraction progress notifications for Linux"
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
dependencies = [
    "PyQt6",
]

[project.urls]
Homepage = "https://github.com/jackharvest/7sense"
